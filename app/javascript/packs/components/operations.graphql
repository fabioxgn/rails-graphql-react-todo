query TodosQuery
{
  todos {
    id
    description
    completed
  }
}

mutation AddTodo($description: String!) {
  addTodo(input: {description: $description}) {
    todo {
      id
      description
      completed
    }
  }
}
